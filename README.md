# MyPass Manager 🛡️

A simple CLI (Command Line Interface) tool I'm building to manage my passwords locally using Python and JSON.

### Current Progress:
- [x] Basic input validation (URL & length)
- [x] Local JSON storage
- [ ] Add encryption (In progress...)
- [ ] Add a search/read function

### Usage:
Just run `python pass.py` and follow the prompts.

*Note: Don't forget to keep your passwords.json safe and don't push it to Git!* `
